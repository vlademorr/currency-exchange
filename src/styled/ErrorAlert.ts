import styled from 'styled-components';

const ErrorContainer = styled.div`
  width: 100%;
  position: absolute;
  z-index: 1;
  margin-top: 80px;
  display: flex;
  justify-content: center;
`;

export {
  ErrorContainer
};
